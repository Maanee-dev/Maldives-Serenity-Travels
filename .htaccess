# Next.js Server Routing Configuration
RewriteEngine On

# Forward all requests to the Node.js process if they aren't real files
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ http://localhost:3000/$1 [P,L]

# Basic security
Options -Indexes